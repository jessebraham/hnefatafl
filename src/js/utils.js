// Team "enum" and corresponding piece colours and human-readable names.
const Teams = Object.freeze({ ATTACKERS: 0, DEFENDERS: 1 });
const TeamColours = ["black", "white"];
const TeamNames = ["Attackers", "Defenders"];

export { Teams, TeamColours, TeamNames };
